<EditForm Model="@movie" OnValidSubmit="@OnValidSubmit">
    <DataAnnotationsValidator />
    <ValidationSummary/>
    <fieldset disabled="@Disabled">
        <div class="form-group">
            <label>Name: </label>
            <div>
                <InputText @bind-Value="@movie.Name" class="form-control" />
                <ValidationMessage For="@(() => movie.Name)" />
            </div>
        </div>

        <div class="form-group">
            <label>Description: </label>
            <div>
                <InputText @bind-Value="@movie.Description" class="form-control" />
                <ValidationMessage For="@(() => movie.Description)" />
            </div>
        </div>
    </fieldset>

    @if(!Disabled)
    {
        <button type="submit" class="btn btn-sucess btn-block">
            @ButtonText
        </button>
    }
    <BackToList Target ="movies" />
</EditForm>

@code {
    [Parameter] public bool Disabled { get; set; } = false;
    [Parameter] public Movie movie { get; set; }
    [Parameter] public string ButtonText { get; set; } = "Save";
    [Parameter] public EventCallback OnValidSubmit { get; set;}
}